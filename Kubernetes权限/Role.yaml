# RoleBinding 将 User 或 ServiceAccount 绑定到 Role
# ClusterRole / ClusterRoleBinding 是跨 Namespace 的权限

apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: dev
  name: pod-reader
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
